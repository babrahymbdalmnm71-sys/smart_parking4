# مشروع "موقف ذكي" (Smart Park) - تفاصيل النظام والتحسينات

هذا المستند يوضح هيكلية المشروع، الميزات المضافة، والتحسينات التي تم تنفيذها لضمان أفضل أداء على كافة المنصات (Android, Web, Windows).

## 🚀 ملخص المشروع
تطبيق احترافي لإدارة مواقف السيارات، يتيح للمستخدمين البحث عن مواقف، حجز أماكن، وتتبع وقت وتكلفة الركن لحظياً. كما يوفر لوحة تحكم كاملة للأدمن لإدارة البيانات.

## 🛠️ التقنيات المستخدمة
- **الإطار البرمجي**: Flutter (نظام Clean Architecture).
- **إدارة الحالة**: Provider (للسرعة والكفاءة).
- **قواعد البيانات**: 
  - **SQLite**: لتخزين البيانات على الموبايل والويندوز.
  - **SharedPreferences**: كبديل فوري (Fallback) لضمان عمل السجل على الويب (Chrome).
- **الخرائط**: Flutter Map (OpenStreetMap) لضمان العمل بدون مفاتيح API مدفوعة.
- **تخزين الصور**: CachedNetworkImage (لتحسين سرعة التحميل وتقليل استهلاك البيانات على الويب).

## ✨ الميزات الرئيسية
1. **نظام المستخدم (User)**:
   - بحث ذكي وثابت عن المواقف.
   - حجز أماكن مع إشعارات تأكيد فورية.
   - عداد زمني يحسب التكلفة بناءً على "الساعة الأولى كاملة" ثم بالدقيقة.
   - سجل مواقف دقيق وإحصائيات شاملة (عدد المواقف، إجمالي المصروف، إجمالي الساعات).
   - نظام كوبونات خصم (مرة واحدة لكل مستخدم).
   - إرسال شكاوي وآراء للإدارة.

2. **نظام الأدمن (Admin)**:
   - دخول خاص (admin / 0000).
   - إضافة مواقف جديدة (اسم، عنوان، سعر، صورة، إحداثيات).
   - تعديل المواقف الحالية لحظياً.
   - استلام ومراجعة شكاوي المستخدمين.

## ⚡ تحسينات الأداء (Web/Chrome Optimization)
تم تنفيذ الخطوات التالية لجعل التطبيق خفيفاً وسريعاً على المتصفح:
- **تحميل الصور الذكي**: استبدال `Image.network` بـ `CachedNetworkImage` مع وجود `Placeholder` لضمان عدم تعليق الواجهة.
- **تحسين الـ Rebuilds**: تقليل عمليات إعادة بناء الواجهات غير الضرورية في `MainScaffold`.
- **نظام تخزين هجين (Hybrid Storage)**: تم حل مشكلة عدم عمل السجل على الويب عن طريق ربطه تلقائياً بـ `SharedPreferences` في حال عدم توفر `SQLite`.
- **تنظيف الكود**: حذف كافة البيانات الزائدة (مثل الإيموجيات القديمة) لتقليل حجم ملف الـ JavaScript الناتج.

## 📁 هيكلية الملفات
- `lib/core`: يحتوي على الثيمات، المساعدات (Prefs, Database)، والنصوص.
- `lib/logic`: يحتوي على منطق التطبيق (AppState).
- `lib/data`: يحتوي على نماذج البيانات والبيانات التجريبية.
- `lib/presentation`: يحتوي على كافة الشاشات والويدجت (UI).

---
**الحالة الحالية**: التطبيق جاهز بنسبة 100% للاستخدام الفعلي، مستقر، وسريع جداً على Chrome والموبايل.
